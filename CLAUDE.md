# Polski Kurwa — тренажёр польского языка

Учебный проект: интерактивные тесты по польскому для самостоятельного изучения (A1 → выше).
Интерфейс и объяснения — **на русском**, материал — на польском.
Живёт на GitHub Pages, ставится на iPhone как приложение.

> Этот файл — рабочая память проекта. Читай его перед каждой задачей и **обновляй**,
> когда меняется структура, добавляется тема или принимается новое решение.

---

## 1. Файлы

Репозиторий = сайт. Всё в корне публикуется как есть.

| Файл | Что это |
|---|---|
| `index.html` | **Всё приложение целиком**: разметка, стили, движок и данные всех тем. Без сборки, без зависимостей. |
| `manifest.webmanifest` | PWA-манифест: имя, иконки, `display: standalone`, цвета. |
| `sw.js` | Service worker. **Сеть первым делом, кэш как запасной вариант** — онлайн всегда свежая версия, офлайн последняя загруженная. Версию бампать не надо. |
| `icons/` | `icon.svg` — исходник (серифная «ę» на терракоте), PNG генерятся из него через `sips` (§7). |
| `deploy.sh` | Публикация одной командой. |
| `backup.sh` | `./backup.sh "почему"` — снимок `index.html` в `backups/`. **Делать перед каждой крупной правкой.** |
| `backups/` | Снимки, последние 15. Откат: `cp backups/index_ДАТА.html index.html`. Плюс git-метки (`git tag -l`). |
| `.nojekyll` | Отключает Jekyll на GitHub Pages. |
| `CLAUDE.md` | Этот файл. |

**Версионирование — через git**, не через имена файлов.

**Все темы живут в одном `index.html`** — движок общий, дублировать его по файлам нельзя.
(Раньше в этом файле стоял план разносить темы по отдельным HTML — от него отказались:
это означало бы копировать ~700 строк движка на каждую тему.)

---

## 2. Архитектура

```
<head>                мета + PWA-теги + запрет зума
<style>               игровая тема: :root токены, панели, кнопки, свайпер, карточки
<body>                HUD (рекорд + очки), #app, модал
<script>
  ОБЩЕЕ               shuffle(), normalize()
  ТЕМА: ГЛАГОЛЫ       PERSON_*, PARADIGMS, EXCEPTIONS, CONJ_GROUPS, VERB_DATA
  ТЕМА: ЧИСЛИТЕЛЬНЫЕ  NUM_SUBTOPICS, NUM_REF, NUM_DATA
  ТЕМА: ПАДЕЖИ        CASE_NOUNS, CASE_ADJ, DAT_PRON, CASE_FRAMES, buildCaseData(), CASE_REF
  РЕЕСТР ТЕМ          TOPICS, TOPIC_ORDER, SUB_EMOJI
  ОЧКИ                levenshtein(), similarity(), gradeAnswer()
  РЕКОРДЫ             loadBest()/saveBest() → localStorage
  ДВИЖОК              buildSwiper() → renderSetup / renderQuiz / renderSummary
```

### Четыре экрана

`topic` (свайп тем → «Выбрать») → `sub` (свайп подтем + число тестов → «Старт») → `quiz` → `summary`.
Переключает `state` + `render()`. У темы без подтем экран `sub` показывает только настройки.

### Свайпер

`buildSwiper(slides, activeIndex, onChange)` → `{html, init(root)}`, `init` возвращает `{settle, jump}`.
Лента на `overflow-x:auto` + `scroll-snap-type:x mandatory`, **во всю ширину экрана**:
у `.swiper` отрицательные поля, равные горизонтальному отступу `body`, слайд 84% + `padding: 0 8%`
у трека, чтобы крайние карточки центрировались. Соседняя карточка выглядывает и уезжает за край.
Стрелок нет — на компьютере переключают кликабельные точки.

Активный слайд ищется **по ближайшему к центру** (`nearest()`), а не делением на ширину:
работает при любых отступах и процентных ширинах.

Два места, где легко наступить на грабли:

- **`settle()` обязан проверять `track.isConnected`.** У открепленного от страницы элемента
  `scrollLeft` равен нулю, и отложенный пересчёт после перерисовки экрана «возвращал» свайпер
  на первый слайд, молча меняя тему уже в середине теста. Ровно этот баг здесь был.
- **Кнопка «Играть» вызывает `settle()` перед стартом.** Событие прокрутки приходит с задержкой,
  и без этого быстрый свайп + сразу тап запускал бы предыдущую тему.

### Очки

`gradeAnswer(q, value)` → `{sim, points, exact, verdict}`.

- Сходство — расстояние Левенштейна: `1 − distance / max(длина)`, берётся лучшее из `answer` и `alt`.
- `points = round(100 × sim)`. Точный ответ — 100, ни одной общей буквы — 0.
- `verdict`: `correct` (точно) · `close` (≥ 60%, показывается «Почти!») · `wrong`.
- Регистр и лишние пробелы не штрафуются (`normalize`). Диакритика штрафуется частично:
  `mowie` вместо `mówię` даёт 60%, а не ноль.
- Итог сессии: `stats.points / stats.maxPoints` → проценты → 1–3 звезды (40/70/90%).
- Рекорд в процентах хранится по теме в `localStorage['polski.best']`, всё в try/catch.

После проверки поле ввода **заменяется на `div.given`** — длинный ответ переносится
и виден целиком, чего input не умеет.

### Откуда берутся вопросы

Большая часть **генерируется на лету при загрузке**, а не лежит списком — иначе файл раздулся бы
до полумегабайта, а поддерживать 3000 строк вручную невозможно.

- `VERB_DATA = VERB_DATA_MANUAL.concat(buildVerbData())` — 122 написанных руками + 3534 собранных
  из `VERB_DEF`. На каждую пару (глагол, дополнение) приходится до 17 вопросов:
  6 утверждений «{Местоимение} ___ {дополнение}», 5 вопросов «Czy …?» (кроме `ja` —
  «Czy ja…» звучит неестественно) и 6 с наречием перед глаголом.
  Дополнения подобраны так, чтобы подходить ко всем шести лицам без изменений.
- **Наречия выбираются не наугад.** `ADV_FREQ` (często/zawsze) — обычные динамические глаголы;
  `ADV_NOW` (teraz/już) — однократное действие и состояния; `ADV_WELL` (dobrze/naprawdę) —
  знание и умение. У `iść`/`jechać` частотное наречие было бы ошибкой: для повторяющегося
  движения нужен `chodzić`/`jeździć`. Где ни одно наречие не ложится (`nazywać się`,
  `dziękować`) — `adv: null`, модель просто не строится.
- `NUM_DATA = NUM_DATA_MANUAL.concat(buildNumData())` — 130 руками + 1231 по правилам.

Числительные считаются, а не перечисляются: `numToPl(n, род)`, `nounForm(n, [ед, им.мн, род.мн])`,
`yearLoc(год)`, таблицы часов. Три правила, на которых легко ошибиться:

- **Форма существительного зависит от числа:** 1 → ед.ч., 2–4 → им.мн. (*dwa lata*),
  остальное → род.мн. (*pięć lat*). Считает `nounForm`.
- **Род работает на «2», но не на «1» в составных:** *dwadzieścia dwie książki*, но
  *dwadzieścia jeden książek* — там `jeden` и родительный. Флаг `oneGender` в `under1000`.
- **В годе склоняется только последний элемент:** *tysiąc dziewięćset osiemdziesiątym piątym*.

Все шаблоны числительных — с переходным глаголом и винительным падежом, поэтому «1» с существительными
женского рода пропускается: винительный там *jedną książkę*, а не *jedna książka*.

### Падежи: система из учебника

Что и как подаётся в «Polski krok po kroku 1» — сверено по поурочному плану издательства
(`rozkład materiału`, Glossa). Во всей книге, включая хвост A2, на существительных пять падежей;
**celownik — только личные местоимения** (урок 17: *podoba mi się, smutno mi*); **wołacz не вводится**.
Существительные в celowniku и wołacz — уже книга 2.

| Ключ | Падеж | В книге |
|---|---|---|
| `M` | Mianownik | мн. число существительных, согласование прилагательных |
| `N` | Narzędnik | *Jestem studentem*, транспорт, *z masłem*, *interesuję się* |
| `B` | Biernik | *lubię, mam, kupuję*, *na koncert* |
| `D` | Dopełniacz | отрицание, *szukam*, *do sklepu*, меры (*kilo sera*) |
| `L` | Miejscownik | *w Krakowie / na poczcie / we Francji*, месяцы, *o czym* |
| `C` | Celownik | только местоимения: *podoba mi się, pomagam ci* |

**Формы существительных написаны руками** (`CASE_NOUNS`, 162 слова: `pl D B N L`, род, одушевлённость,
предлог места `w/we/na`, теги категорий). Генерировать их по правилам нельзя — dopełniacz
неодушевлённых (*domu*, но *chleba*), беглое *e*, чередования *ó→o*, *a→e* непредсказуемы.
Зато наивное правило работает как **контрольный список**: скрипт в §5 печатает каждую форму,
где таблица расходится с регулярным окончанием. Все расхождения обязаны быть известными
нерегулярностями — новое расхождение проверять глазами.

**Рамки** (`CASE_FRAMES`): глагол требует падеж, существительное подбирается по тегам
(*jem* — только `food`, *jadę* — `transport`, *idę do* — всё кроме слов с предлогом `na`).
Пропуск — только склоняемое слово; где тренируется *w/na*, ответ идёт с предлогом (*w Krakowie*).
Глагольные формы берутся из `PARADIGMS` темы «Глаголы», недостающие — в `CASE_VERBS`.
Предикативный narzędnik (*Jestem studentem*) — только ед. число, `On` с мужским словом,
`Ona` с женским. Ответы с ошибкой показывают слово и род: *Poznań (м. р.) → Poznaniu*.

У темы свои эмодзи подтем через `topic.subEmoji` — ключи `M` и т. п. пересекаются
с подтемами глаголов в общей карте `SUB_EMOJI`, поэтому в `renderSubScreen` сначала
смотрится карта темы.

### Рейтинг и прогресс

`localStorage['polski.progress.v1']` — записи по ключу `тема` и `тема:подтема`
(`recKey(topicId, subId)`, без подтемы → `тема:all`): `{points, best, played}`.
Уровень считается от `best`: 🌱 Новичок < 40 · 📗 Ученик < 60 · 💪 Уверенно < 80 · 🎓 Знаток < 95 · 👑 Мастер.
Показывается на карточках выбора (`pickCard`) и в итогах. Всё в try/catch.

### Спряжения: система из учебника

Группы названы **по окончаниям ja/ty, а не римскими цифрами** — ровно как в «Polski krok po kroku 1»
(издательство Glossa). Проверено по учебнику и по блогу издателя: в настоящем времени там
**три спряжения плюс `być` отдельно**:

| Ключ | Подпись | Образцы в книге | Окончания ja / ty |
|---|---|---|---|
| `M` | `-m, -sz` | mieć, rozumieć, czytać | `-am/-em` · `-asz/-esz` |
| `ISZ` | `-ę, -isz/-ysz` | robić, tańczyć | `-ę` · `-isz/-ysz` |
| `ESZ` | `-ę, -esz` | pisać, gotować | `-ę` · `-esz` |
| `EXC` | `być` | — | нерегулярный |

Группа `M` объединяет `-am/-asz` (czytam) и `-em/-esz` (jem, wiem, rozumiem) — так в книге.
Обе схемы показаны в колонке окончаний и в справочной таблице.

Раньше здесь была самодельная нумерация A–E, где I и III стояли местами относительно
польской традиции. Она заменена целиком: нумерации больше нет, спорить не с чем.

### Микс подтем

`mixSet` — набор подтем, отмеченных кнопкой «＋ в микс» прямо на карточке свайпера.
Пока он не пуст, тест идёт **по нему**, а не по карточке в центре (`subsSet()`),
кнопка становится «СТАРТ · МИКС (N)», над настройками появляется жёлтая панель с составом.

Три места, где микс сбрасывается, — и все три обязательны:

- смена темы (`onChange` темного свайпера);
- свайп на «Всё подряд» — оно и так всё, микс там лишний;
- **вход на экран тем (`renderTopicScreen`)** — иначе микс переживает возврат из итогов
  и молча подменяет то, на что человек свайпнул. Этот баг здесь был.

Прогресс микса пишется под ключ `тема:mix` (одна общая запись на любые сочетания),
одиночная подтема — под свой ключ. См. `currentRecKey()`.

### Контракт темы

Движок не знает про глаголы и числительные — только про общий формат вопроса:

```js
{
  g:        'ключ группы',      // статистика и разбор ошибок
  hint:     'подпись',          // пилюля в шапке карточки
  sentence: 'Ona ___ miła.',    // ровно один пропуск ___
  answer:   'jest',
  alt:      ['вариант'],        // необязательно
  ref:      'ключ справки'      // что открыть в модале
}
```

Сама тема в `TOPICS`:

```js
{
  id, title, subtitle,
  emoji: '🗣️',                   // в кружке на карточке выбора
  color: 'green' | 'amber',       // класс .pick-c-<color>
  level: 'A1',
  subtopics: [{id,label,caption}] | null,   // null = свайпера подтем нет
  note:     'строка под кнопкой Играть',
  blockInput: false,              // true = поле под предложением на всю ширину

  questions(subs),                // subs = Set выбранных подтем или null
  groupLabel(key),
  wrongHtml(q),                   // БЕЗ ведущей иконки — её ставит движок (✕ или ≈)
  adviceHtml(key),
  reference(refKey)               // → {title, tag, sub, cols, rows}
}
```

Подтемы выбираются **по одной** (свайпер), первым слайдом идёт «Всё подряд» (`id: null`) —
он и даёт смешанную тренировку.

### Как добавить тему

1. Блок данных в `<script>` рядом с остальными.
2. Объект в `TOPICS` по контракту выше.
3. `id` в `TOPIC_ORDER`.
4. Класс `.pick-c-<color>` в стилях, если нужен новый цвет.
5. Эмодзи подтем — в `SUB_EMOJI`.

## 3. Правила при изменениях

1. **Перед крупной правкой — `./backup.sh "что собираюсь делать"`.** Плюс git-метка на удачную версию: `git tag -f vN-описание`. Откат: `cp backups/index_ДАТА.html index.html`.
2. **Ноль зависимостей и ноль сборки.** Никаких CDN и npm. Любой внешний файл сломает офлайн и его придётся вносить в `ASSETS` в `sw.js`.
3. **Зум запрещён намеренно** — `user-scalable=no` в `<meta viewport>`, `touch-action:manipulation` в CSS (двойной тап) и `preventDefault` на `gesture*` (щипок в Safari). Не «чинить» это как баг.
4. **Инвариант глаголов:** `answer === PARADIGMS[v][PERSON_ORDER.indexOf(person)]`; каждый глагол либо в `EXCEPTIONS`, либо ровно в одной группе `CONJ_GROUPS`, **и его формы совпадают с окончаниями этой группы** (проверка в §5). Спряжения именуются по учебнику Krok po kroku, римских цифр не вводить.
5. **Инвариант числительных:** `sub` есть в `NUM_SUBTOPICS`, для него есть таблица в `NUM_REF`, в предложении ровно один `___`.
6. После правки данных — прогнать проверку целостности (§5).
7. **Подтема должна тянуть на полный тест** — минимум 10 вопросов, иначе её нельзя выбрать одну.
8. **Стиль UI — игровой:** только токены из `:root`, шрифт `ui-rounded`, объёмные кнопки (`.gbtn` с `box-shadow: 0 5px 0 <тёмный>`, при нажатии уезжают вниз), панели с рамкой и внутренним бликом, эмодзи вместо иконок. Мобильный — одна колонка (`max-width:620px`).
9. **Ширина поля ввода не зависит от длины ответа** — иначе она его подсказывает.
9а. **В падежах пропуск — только склоняемое слово** (плюс предлог `w/we/na`, когда тренируется место). Существительное с другой стороны рамки уже стоит в нужной форме. Новое слово в `CASE_NOUNS` — все пять форм руками, затем §5.
10. **Язык:** UI и объяснения по-русски, примеры по-польски, уровень A1.
11. **Иконки не редактировать вручную** — править `icons/icon.svg` и перегенерировать (§7).
12. После правок — `./deploy.sh "что изменил"`.

### Отдельно про содержание числительных

Числительные в польском управляют падежом существительного, поэтому **пропуск всегда только на само числительное**, а существительное уже стоит в предложении в правильной форме. При добавлении вопросов сверяй:

- 1 → единственное число: *jeden złoty*
- 2, 3, 4 и составные на них → именительный мн.: *dwadzieścia dwa **lata**, sześćdziesiąt cztery **osoby***
- 5–21 и всё остальное → родительный мн.: *dwadzieścia pięć **lat**, sto jeden **złotych***
- после 2/3/4 тысяч — *tysiące*, после 5+ — *tysięcy*

---

## 4. Текущее состояние контента

| Тема | Вопросов | Из них руками | Подтемы |
|---|---|---|---|
| Глаголы | 3656 | 122 | 4 (три спряжения + `być`), **55 глаголов** |
| Числительные | 1361 | 130 | 6 |
| Падежи | 5225 | формы 162 слов | 6 (пять падежей + celownik на местоимениях) |
| **Всего** | **10 242** | | |

**Глаголы по спряжениям:** `-m, -sz` 1455 · `-ę, -isz/-ysz` 897 · `-ę, -esz` 788 · `być` 516.
В каждом больше 500, как и просил заказчик.

**Числительные.** Цифры 108 · Десятки 206 · Сотни 220 · Тысячи 253 · Даты 275 · Время 299.

**Падежи.** Mianownik 528 · Narzędnik 903 · Biernik 1020 · Dopełniacz 1245 · Miejscownik 1017 · Celownik 512.

### Известные шероховатости (не трогать без запроса)

- Сгенерированные предложения однотипны по структуре («{Местоимение} ___ {дополнение}»,
  «Czy …?», «{Местоимение} {наречие} ___ {дополнение}») — берут разнообразием словаря,
  а не синтаксиса. Руками написанные 252 предложения живее.
- Правильность польского в сгенерированном держится на правилах и юнит-проверках,
  а не на носителе языка. Если всплывёт кривая фраза — чинить правило, а не отдельный вопрос.
- Хвост вопросов после деления на десятки в сессию не попадает (остаток от деления на 10).
- Диакритика сверяется строго, но частичное совпадение даёт очки: `mowie` вместо `mówię` — 60%.
- `AGENTS.md` в корне — почти точная копия `CLAUDE.md` (расходится одна строка).
  Не синхронизируется автоматически, при правках этого файла легко разъезжается.

## 5. Проверка целостности данных

Прогонять после любой правки данных или генераторов. Сверяет ответы с парадигмами,
членство глаголов в группах, единственный пропуск в предложении и наполненность подтем.

```bash
python3 - <<'EOF'
import io,subprocess
s=io.open('index.html',encoding='utf-8').read()
js=s[s.index('/* ============ ОБЩЕЕ ============ */'):s.index('/* ============ РЕЕСТР ТЕМ')]+"""
let bad=0; const idx=p=>PERSON_ORDER.indexOf(p); const seenV=new Set();
VERB_DATA.forEach((q,i)=>{
  seenV.add(q.v); const f=PARADIGMS[q.v];
  if(!f){console.log('нет парадигмы:',q.v);bad++;return;}
  if(f.length!==6){console.log('не 6 форм:',q.v);bad++;}
  if(f[idx(q.person)]!==q.answer){console.log('форма≠ответ:',i,q.v,q.person);bad++;}
  if(q.sentence.split('___').length-1!==1){console.log('пропуск:',i);bad++;}
  const exc=EXCEPTIONS.has(q.v), grp=Object.values(CONJ_GROUPS).filter(G=>G.verbs.includes(q.v)).length;
  if(exc&&grp){console.log('и исключение и группа:',q.v);bad++;}
  if(!exc&&grp!==1){console.log('групп у глагола:',q.v,grp);bad++;}
});
Object.keys(VERB_TAILS).forEach(v=>{if(!PARADIGMS[v]){console.log('дополнения без парадигмы:',v);bad++;}});
const ids=new Set(NUM_SUBTOPICS.map(x=>x.id)), per={};
NUM_DATA.forEach((q,i)=>{
  per[q.sub]=(per[q.sub]||0)+1;
  if(!ids.has(q.sub)||!NUM_REF[q.sub]){console.log('подтема:',i,q.sub);bad++;}
  if(q.sentence.split('___').length-1!==1){console.log('пропуск:',i);bad++;}
  if(!q.answer||/undefined|  |^ | \$/.test(q.answer)){console.log('ответ:',i,JSON.stringify(q.answer));bad++;}
  if(!q.hint){console.log('нет подсказки:',i);bad++;}
});
NUM_SUBTOPICS.forEach(x=>{if((per[x.id]||0)<10){console.log('меньше 10:',x.id);bad++;}});
console.log('глаголы',VERB_DATA.length,'('+seenV.size+' слов) · числительные',NUM_DATA.length,JSON.stringify(per));
console.log(bad? bad+' ПРОБЛЕМ':'✓ все проверки прошли');
"""
subprocess.run(['node','-e',js])
EOF
```

Отдельно — **совпадают ли формы глагола с окончаниями заявленного спряжения**.
Это главная защита от того, чтобы глагол попал не в ту группу:

```bash
python3 - <<'EOF'
import io,subprocess
s=io.open('index.html',encoding='utf-8').read()
js=s[s.index('const PERSON_LABEL'):s.index('const VERB_DATA_MANUAL')]+"""
const PAT = {
  M:   [/(am|em)$/, /(asz|esz)$/, /(a|e)$/, /(amy|emy)$/, /(acie|ecie)$/, /(ają|dzą|eją)$/],
  ISZ: [/ę$/, /(isz|ysz)$/, /(i|y)$/, /(imy|ymy)$/, /(icie|ycie)$/, /ą$/],
  ESZ: [/ę$/, /esz$/, /e$/, /emy$/, /ecie$/, /ą$/]
};
const strip = f => f.replace(/ się$/,'');
let bad=0;
Object.entries(CONJ_GROUPS).forEach(([k,g])=>g.verbs.forEach(v=>{
  const f=PARADIGMS[v];
  if(!f){console.log('нет парадигмы:',v);bad++;return;}
  f.forEach((form,i)=>{ if(!PAT[k][i].test(strip(form))){
    console.log('✗ форма не по группе:',k,v,PERSON_ORDER[i],form);bad++; } });
}));
Object.keys(PARADIGMS).forEach(v=>{
  const inG=Object.values(CONJ_GROUPS).filter(g=>g.verbs.includes(v)).length;
  if(EXCEPTIONS.has(v)? inG : inG!==1){console.log('✗ группа:',v,inG);bad++;}
  if(!VERB_DEF[v]){console.log('✗ нет дополнений:',v);bad++;}
});
console.log(bad? bad+' ПРОБЛЕМ':'✓ спряжения OK — '+Object.keys(PARADIGMS).length+' глаголов');
"""
subprocess.run(['node','-e',js])
EOF
```

Отдельно — **падежи**: наивное правило склонения печатает каждое расхождение с таблицей.
Это контрольный список, а не pass/fail: все строки должны быть известными нерегулярностями
(беглое *e*, *ó→o*, *a→e*, *-u* у *dom/syn/pan*, «женское» склонение *kolega/dentysta*).
Незнакомая строка — повод открыть словарь.

```bash
python3 - <<'EOF'
import io,subprocess
s=io.open('index.html',encoding='utf-8').read()
js=s[s.index('const CASE_NOUNS'):s.index('const CASE_ADJ')]+"""
const soft=st=>/(c|dz|cz|sz|ż|rz|l|j|ś|ź|ć|ń)$/.test(st);
const SOFT=[[/st$/,'ści'],[/sł$/,'śl'],[/k$/,'c'],[/g$/,'dz'],[/ch$/,'sz'],[/r$/,'rz'],[/t$/,'ci'],[/d$/,'dzi'],[/ł$/,'l'],[/n$/,'ni'],[/m$/,'mi'],[/p$/,'pi'],[/b$/,'bi'],[/w$/,'wi'],[/s$/,'si'],[/z$/,'zi']];
const sE=st=>{for(const [re,rep] of SOFT) if(re.test(st)) return st.replace(re,rep)+'e'; return st+'e';};
let d=[];
CASE_NOUNS.forEach(x=>{const e={};
  if(x.g==='f'&&/a$/.test(x.n)){const st=x.n.slice(0,-1);e.B=st+'ę';e.N=st+'ą';e.D=/[kg]$/.test(st)?st+'i':(soft(st)?st+'y':st+'y');e.L=soft(st)?e.D:sE(st);}
  else if(x.g==='n'&&/[oe]$/.test(x.n)){const st=x.n.slice(0,-1);e.D=st+'a';e.N=/[kg]$/.test(st)?st+'iem':st+'em';e.L=/e$/.test(x.n)||/[kg]$/.test(st)?st+'u':sE(st);}
  else if(x.g==='m'&&!/[aiy]$/.test(x.n)){e.N=/[kg]$/.test(x.n)?x.n+'iem':x.n+'em';e.L=(/[kg]$|ch$/.test(x.n)||soft(x.n))?x.n+'u':sE(x.n);if(x.an)e.D=x.n+'a';}
  Object.entries(e).forEach(([k,v])=>{if(x[k]!==v)d.push(x.n.padEnd(13)+k+': '+x[k].padEnd(14)+'правило '+v);});
  ['D','B','N','L'].forEach(k=>{if(!x[k])d.push(x.n+' НЕТ ФОРМЫ '+k);});
});
console.log('слов:',CASE_NOUNS.length,'· расхождений:',d.length); d.forEach(l=>console.log(' ',l));
"""
subprocess.run(['node','-e',js])
EOF
```

Отдельно — генератор числительных (контрольные формы, которые легко испортить):

```bash
node -e "$(python3 -c "
import io;s=io.open('index.html',encoding='utf-8').read()
print(s[s.index('/* ===== числительные словами'):s.index('const N_NOUNS')])
")
let bad=0;
[[1,'f','jedna'],[21,'f','dwadzieścia jeden'],[22,'f','dwadzieścia dwie'],[101,'m','sto jeden'],
 [1000,'m','tysiąc'],[2024,'m','dwa tysiące dwadzieścia cztery'],[21000,'m','dwadzieścia jeden tysięcy']]
 .forEach(([n,g,e])=>{const v=numToPl(n,g); if(v!==e){console.log('✗',n,g,v,'≠',e);bad++;}});
[[1918,'tysiąc dziewięćset osiemnastym'],[2000,'dwutysięcznym'],[2010,'dwa tysiące dziesiątym']]
 .forEach(([y,e])=>{const v=yearLoc(y); if(v!==e){console.log('✗',y,v,'≠',e);bad++;}});
[[1,'rok'],[2,'lata'],[5,'lat'],[22,'lata'],[112,'lat']]
 .forEach(([n,e])=>{const v=nounForm(n,['rok','lata','lat']); if(v!==e){console.log('✗',n,v,'≠',e);bad++;}});
console.log(bad? bad+' ОШИБОК':'✓ генератор чисел OK');
"
```

## 6. Куда расширять

- **Числительные:** порядковые (pierwszy/drugi…), «ile masz lat», цены с гроszami, номера телефонов, собирательные (dwoje, troje).
- **Глаголы:** добить `my`/`wy`, довести каждый глагол до 6 форм, добавить A1-глаголы (`wracać`, `dzwonić`, `oglądać`, `sprzątać`, `myć się`, `budzić się`, `nazywać się`, `wstawać`, `brać`, `dawać`).
- **Падежи:** прилагательные в косвенных падежах (*z dobrym kolegą*), мн. число в biernik/dopełniacz (*kilo jabłek*), местоимения в biernik/dopełniacz (в книге есть: *znam go, nie ma jej*). Celownik на существительных и wołacz — книга 2, не тащить в A1.
- **Новые темы (каждая — объект в `TOPICS`):** прошедшее время, предлоги, притяжательные, вид глагола.
- **Механика:** режим повторения только ошибок, прогресс в `localStorage`, мягкая проверка диакритики с предупреждением, обратный перевод RU→PL, озвучка через Web Speech API.

---

## 7. iPhone: установка и обновление

**URL сайта:** https://elmishgan.github.io/polski/
**Репозиторий:** https://github.com/elmishgan/polski (публичный — бесплатный Pages иначе не работает)

### Разовая установка

1. Открыть URL в **Safari** (Chrome на iOS не умеет ставить на домашний экран).
2. «Поделиться» → **«На экран «Домой»»** → «Добавить».
3. Иконка «Polski». Полный экран, без адресной строки, работает офлайн, зум отключён.

### Обновление

```bash
./deploy.sh "что изменил"
```

Через ~30 секунд GitHub Pages пересобирается. На телефоне достаточно закрыть и снова
открыть иконку **при наличии сети**. Офлайн отдаётся последняя загруженная версия.

Если версия застряла: «Настройки → Safari → Очистить историю и данные» либо удалить
иконку и поставить заново.

### Локальная проверка

Service worker требует `https://` или `localhost` — с `file://` не зарегистрируется
(приложение откроется, но без офлайн-кэша).

```bash
python3 -m http.server 8765
```

### Перегенерация иконок

```bash
for s in 180 192 512; do sips -s format png -Z $s icons/icon.svg --out icons/icon-$s.png; done
sips -s format png -Z 512 icons/icon-maskable.svg --out icons/icon-maskable-512.png
```

`icon-maskable.svg` — та же «ę», но мельче: у maskable-иконок края обрезаются,
содержимое должно помещаться в центральные 80%.
